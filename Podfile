# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '16.4'
use_frameworks!
pod 'Starscream', '~> 4.0.0'

use_frameworks!


target 'Acucent iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

pod 'Socket.IO-Client-Swift', '~> 16.0.0' # or a newer version if available
pod 'Socket.swift', '~> 2.4.0'
  # Pods for Acucent iOS

  target 'Acucent iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Acucent iOSUITests' do
    # Pods for testing
  end

end
